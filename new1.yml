pipelines:
  - name: test_pipe_
    configuration:
      retentionPolicy:
        maxAgeDays: -2
        minRuns: 5
    steps:
      - name: testStep
        type: Bash
        execution:
          onStart:
            - echo "executing onStart"
          onExecute:
            - echo "executing onExecute"
